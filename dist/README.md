This directory contains scripts to easily call runsql for different
platforms. Add one of the `bin` directories to your path. After
running `build`, you then should be able to execute `runsql -h` from
the command line.